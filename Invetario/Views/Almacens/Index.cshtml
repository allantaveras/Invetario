@model IEnumerable<Invetario.Models.Almacen>

@{
    ViewBag.Title = "Almacenes";
}

<h2>Almacenes</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descripcion_almacen)
        
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion_almacen)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_almacen }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_almacen }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_almacen })
        </td>
    </tr>
}

</table>
